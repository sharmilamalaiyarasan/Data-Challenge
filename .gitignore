# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
*.env

# ── Cache (auto-generated on first run — do NOT commit) ──────────────────────
cache/

# ── Local model weights (large binary — download on first run) ───────────────
models/

# ── Intermediate output ───────────────────────────────────────────────────────
output/

# ── Large data files (not needed in repo — referenced locally) ───────────────
candidates.jsonl
exhaustive_honeypots.json
signal_anomalies.json

# ── OS artefacts ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Jupyter / IPython ────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── IDE / editor ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
